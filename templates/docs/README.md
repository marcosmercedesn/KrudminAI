# Host-App Documentation Package

This is the source template for the future install generator's `docs/krudmin_ai/` package. The generator and idempotent docs-sync behavior are deferred to the generator vertical slice.