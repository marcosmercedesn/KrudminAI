<div class="page-heading">
  <div><p class="eyebrow"><%= @dashboard.label %></p><h1>Operations dashboard</h1><p class="lede">Live ticket visibility for the current tenant and policy scope.</p></div>
  <div><%= link_to "Refresh", dashboard_refresh_path, class: "text-action", data: { turbo_stream: true } %> <%= link_to "New ticket", new_ticket_path, class: "button" %></div>
</div>
<div id="dashboard-widgets"><%= render "widgets", widgets: @widgets %></div>